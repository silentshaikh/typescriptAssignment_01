let car:string = 'Bently';
console.log("Is car === Bently? I predict True.")
console.log(car === 'Bently');
console.log("Is car.toUpperCase() === BENTLY   ? I predict True.")
console.log(car.toUpperCase() === 'BENTLY');
console.log("Is car.toLowerCase() === bently? I predict True.")
console.log(car.toLowerCase() === 'bently');
console.log("Is car.length ===6 ? I predict True.")
console.log(car.length === 6)
console.log("Is car !== Ferrari? I predict True.")
console.log(car !== 'Ferrari');
console.log("Is car !== Bently? I predict False.")
console.log(car !== 'Bently');
console.log("Is car.toUpperCase() !== BENTLY   ? I predict False.")
console.log(car.toUpperCase() !== 'BENTLY');
console.log("Is car === Ferrari? I predict False.")
console.log(car === 'Ferrari');
console.log("Is car.length !==6 ? I predict False.")
console.log(car.length !== 6)
console.log("Is car.toLowerCase() !== bently? I predict False.")
console.log(car.toLowerCase() !== 'bently');